name: "multi event pipeline"

on: 
    push:
        branches:
            - main
            - tank

    pull_request:
        branches:
            - main
            - tank

jobs:
    on-push:
        runs-on: ubuntu-latest
        steps:
            - name: "master push"
              run: echo "master push"

    on-pull-request:
        runs-on: ubuntu-latest
        steps:
            - name: "master push"
              run: echo "master push"

            